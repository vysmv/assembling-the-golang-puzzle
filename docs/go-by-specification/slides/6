- Как один готовый символ (одна «кодовая точка»). 
Это так называемые *precomposed* (предварительно составленные): 
где один code point для `ä` — `U+00E4`.

- Как комбинацию базовой буквы и отдельного знака акцента (две «кодовые точки»). 
*Combining sequences* (комбинированные): 
два кода — буква `a` (`U+0061`) + символ диерезиса `¨` (`U+0308`).