- CamelCase (например, SliceType), — это нетерминал (ссылка на другой Production).

- lowercase (например, identifier), — это терминал. То есть это уже не поддающийся разложению элемент.

- "текст" в двойных кавычках — это тоже терминал.  
  Такой терминал мы буквально пишем в коде (например, func, struct, {).