- Как один готовый символ (одна «кодовая точка»).  
  Это так называемые *precomposed* символы (предварительно составленные).  
  Они представляют один code point для `a` + `¨` — `U+00E4` (`ä`).

- Как комбинация базовой буквы и отдельного знака акцента (то есть как две «кодовые точки»).  
  Такие символы называются *combining sequences* (комбинированные).  
  Они представляют собой два кода: буква `a` (`U+0061`) + символ диерезиса `¨` (`U+0308`).
