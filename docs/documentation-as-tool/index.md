# Изучение Go. Ч.2: документация как инструмент

Привет всем заглянувшим.

В этом видео я каснусь темы «документации».  
На первом этапе я не собирался записывать об этом ролик, но потом мне показалось, что кому-то это всё-таки может быть интересно.  
Потому что из разбора того, как пользоваться документацией, я, например, сделал для себя интересные выводы о Go в целом.  
И, возможно, кому-то из вас это тоже будет интересно.

## Общая концепция

Я по своей привычке попытался создать для себя понимание общей концепции.  
Так как основной опыт работы у меня был с PHP, я ожидал увидеть что-то подобное php.net.

Но логика go.dev оказалась другой. Она требует понимания устройства Go или, можно сказать, отражает устройство Go.

Если посетить [официальный сайт](https://go.dev/), то можно увидеть, что в своей навигации он ведёт как на свои страницы, так и на другие ресурсы.

Выглядит это примерно так:
 
- [Why Go](#)
    - [Case Studies](https://go.dev/solutions/case-studies)  
    - [Use Cases](https://go.dev/solutions/use-cases)  
    - [Security](https://go.dev/security/)
- [Learn](https://go.dev/learn/)
- [Docs](https://go.dev/learn/#)
    - [Go Spec](https://go.dev/ref/spec)
    - [Go User Manual](https://go.dev/doc)
    - [Standard library](https://pkg.go.dev/std)
    - [Release Notes](https://go.dev/doc/devel/release)
    - [Effective Go](https://go.dev/doc/effective_go)
- [Packages](https://pkg.go.dev/)
- [Community](https://go.dev/learn/#)
    - [Recorded Talks](https://go.dev/talks/)
    - [Meetups](https://www.meetup.com/pro/go)
    - [Conferences](https://go.dev/wiki/Conferences)
    - [Go blog](https://go.dev/blog)
    - [Go project](https://go.dev/help)

Тут мы видим верхнеуровневые разделы «Почему Go», «Обучение», «Документация», «Пакеты» и «Сообщество». Также есть набор вложенных адресов в разделах.  
В общем, проведя визуальный осмотр ссылок навигации и ссылок на внутренних страницах, я не сразу смекнул, в чём тут смысл (есть ссылки на go.dev, pkg.go.dev и сторонние ресурсы типа meetup.com), но, немного покопав, я сделал для себя следующие выводы:

1. Спецификация по адресу https://go.dev/ref/spec — это полное описание языка. То есть это и есть весь язык Go, вне контекста его применения.

Спецификация описывает весь язык Go от начала и до конца.  
То есть:

- синтаксис
- ключевые слова
- выражения
- типы
- интерфейсы
- структуры
- дженерики
- пакеты и импорты
- области видимости
- правила выполнения
- модель памяти
- ...

И можно сделать общий вывод, что:  
**📌 Если чего-то нет в спецификации — этого нет в языке.**

Также интересно, что в Go есть лишь небольшой набор встроенных функций, которые вынесены в псевдопакет `builtin`.  
Их описание можно найти по адресу https://pkg.go.dev/builtin.  
Но они описываются и в спецификации, в разделе `Built-in functions`.

Этот пакет НЕ нужно импортировать — то есть он логически принадлежит языку, а не стандартной библиотеке.

Этим мы полностью закрываем вопрос языка, так как определили его источник истины. 

2. Далее есть страница https://pkg.go.dev/, которая позволяет искать по всем публичным пакетам в экосистеме Go.  
Разработчиком этих пакетов может быть любой человек.  
В качестве результата поиска будут ссылки на страницу с описанием пакета.

То есть можно увидеть, какие типы, функции, методы и константы он добавит в приложение, если его импортировать.  
Иными словами, это объединённые по смыслу наборы из типов, функций, методов и констант, созданных по правилам спецификации (языка) и доступных для импорта.

То есть можно сделать общий вывод:  
**📌 `pkg.go.dev` ищет по всем публичным пакетам экосистемы Go, включая стандартную библиотеку.**

3. Есть страница https://pkg.go.dev/std — это то же самое, что и предыдущий раздел, но здесь представлены только те пакеты, которые разрабатывает `Go team`.  
Все они идут из коробки и всегда доступны. Версии этих пакетов жёстко связаны с версией Go. То есть, в отличие от внешних пакетов, их версии не выбираются отдельно.

То есть можно сделать общий вывод, что:  
**📌 `pkg.go.dev/std` — это фильтр или срез того же самого каталога, который показывает только стандартные пакеты.**

4. Следующий важный слой — это документация для Toolchain Go (который доступен через бинарник `go`).  
Она доступна через вызов `go help` или по адресу https://pkg.go.dev/cmd/go.  
Я это упоминал в прошлом видео, когда разбирал тулчейн Go.

Из всего вышесказанного можно сделать резюме:

Go = Language + Packages + Tooling

Language:
  - fully described in the [Go Spec](https://go.dev/ref/spec) (https://pkg.go.dev/builtin);
  - includes a small set of functions and types exposed via the pseudo-package `builtin`;

Packages:
  - [standard library](https://pkg.go.dev/std);
  - [third-party packages](https://pkg.go.dev).

Tooling (a separate layer, not part of the language) ([docs](https://pkg.go.dev/cmd/go)):
  - `go build`;
  - `go test`;
  - `go mod`;
  - ...

Go — это совокупность языка, пакетов на нём написанного кода и тулчейна разработчика.  
А ядро документации Go — это описание языка в спецификации по адресу `go.dev/ref/spec` + отдельная страница с описанием псевдопакета `builtin` по пути `pkg.go.dev/builtin`.  
Далее — это страница описания пакетов стандартной библиотеки по адресу `pkg.go.dev/std`.  
Страница поиска пакетов по всей экосистеме Go расположена по адресу `pkg.go.dev`.  
А также страница с документацией к тулчейну Go — по адресу `pkg.go.dev/cmd/go`.

Остальное — это туториалы, best practices, ссылки на авторитетные ресурсы и т. п.  
То есть это тоже важно, но не первично.

Для полной картины нам остаётся разобраться с тем, как читать описание пакетов из коллекции std и любых сторонних.  
Далее определить, есть ли разница в логике описания.  
И в завершение сделать небольшую заметку о том, как работать с документацией из терминала и посмотреть несколько полезных ссылок.

## Как читать описания пакетов

Давайте пойдём по порядку.  
И начнём с того, «как читать описания пакетов».

Для примера я возьму:

- стандартный пакет `fmt` — максимально простой и знакомый;
- сторонний пакет `github.com/pkg/errors` — маленький, без перегруза.

Как показал мой опыт,  
любая страница пакета на `pkg.go.dev` имеет одну и ту же структуру:

- **Overview** — общий обзор пакета. Что-то типа комментария к пакету, описывающего общее назначение пакета и иногда — ключевые концепции или ограничения. Это единственное место, где автор пакета объясняет, зачем пакет существует.  
  Никакой другой раздел этого не делает.
- **Index** — это оглавление экспортируемых сущностей пакета, а именно: `Constants`, `Variables`, `Functions`, `Types`. Index — это не описание, а карта API.
- **Examples** — не всегда заполнен, но, если есть, то содержит примеры применения.
- **Constants** — детальное описание только констант, перечисленных в Index.
- **Variables** — детальное описание только переменных, перечисленных в Index.
- **Functions** — детальное описание только функций, перечисленных в Index.
- **Types** — детальное описание только типов (`struct`, `interface`, `type alias`, `defined types`), перечисленных в Index, и их методов (методы в Go объявляются не для значений и не для переменных, а для определённых типов — `defined types`, но об этом в другой раз).
- **Source Files** — файлы с исходным кодом пакета.

При работе с внешними пакетами есть небольшое UX-отличие. Так как у внешнего пакета есть дополнительные сущности типа `README`, `LICENSE`, `CHANGELOG`, то нужно в левом сайдбаре кликнуть на **Documentation** — это переход именно к API-reference, которое будет соответствовать вышеописанной структуре.

## Документация в терминале

И теперь посмотрим — как работать с документацией из терминала.  
Я рассказываю это просто как дополнение.

Go позволяет получать документацию прямо из терминала с помощью команды `go doc`.

Например, получить краткое описание стандартного пакета можно через команду:

```bash
go doc -short fmt
```

И также можно посмотреть описание пакета стороннего разработчика (если он уже скачан локально):

```bash
go doc -short github.com/go-mysql-org/go-mysql/replication
```
У меня он не скачан.  
Поэтому, если я выполню команду, то это выдаст ошибку.

Как вариант, можно получить полное описание пакета, заменив `short` на `all`:
```bash
go doc -all github.com/go-mysql-org/go-mysql/replication  
```

Или

```bash
go doc -all fmt
```

Также Go позволяет показать описание, например, конкретной функции, входящей в состав пакета. Для этого можно просто указать её название через точку после имени пакета:

```bash
go doc fmt.Append
```

Напомню, что все возможности можно узнать по адресу pkg.go.dev/cmd/go.

## Полезные ссылки

Ну и напоследок добавлю несколько ссылок с оф. сайта, которые мне показались интересными, и, возможно, вы тоже найдёте там что-то для себя интересное.

1. [Go Web Examples](https://gowebexamples.com/) provides easy-to-understand code snippets demonstrating how to use Go for web development.
2. A [master class](https://spf13.com/presentation/building-an-awesome-cli-app-in-go-oscon/) that introduces building CLI applications in Go, as well as the core principles of Go and CLI frameworks.
3. [Go by Example](https://gobyexample.com/) is a hands-on introduction to the Go programming language using annotated example programs.
4. [Tips for writing clear, efficient, and idiomatic Go code](https://go.dev/doc/effective_go)

Я, конечно, провёл лишь беглый осмотр этих страниц, но они показались мне любопытными.

На этом всё. Тему считаю завершённой. Если видео было вам интересно, то не забывайте о подписке и лайке. Всем спасибо за просмотр и пока.

 

