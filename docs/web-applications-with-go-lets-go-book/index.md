
# Веб-приложения на Go (книга Let's Go)

## Введение

Привет всем заглянувшим.

В этом видео я хочу поделиться с вами книгой, в которой рассматривается использование Go для веба.  
Я сам ее недавно прочел, и книга мне, в общем-то, понравилась.  
Текст свежий. Используется версия 1.26, и объяснения достаточно подробные.  

В общем, на мой взгляд, если вы в начале пути освоения Go для разработки веб-приложений, то вам будет интересно. Если вы уже в этой нише год или, тем более, больше, то уже сомнительно.  
Но можете пройтись по содержанию — вдруг что-то заинтересует.

Текст книги и ссылки на GitHub будут в моем ТГ-канале.

При этом замечу, что сама книга на английском, но так как доступна HTML-версия книги, то вы спокойно можете читать ее перевод в вашем браузере, если вам так удобнее.

В общем, в книге будет создано небольшое приложение, и на его примере будут рассмотрены основные концепции.

Если сделать беглый обзор, то картина будет такая:

1. **UI**

У нас есть главная страница `https://localhost:4000/`, которая просто выводит список заголовков, дат и ID существующих сниппетов.

Заголовок является ссылкой на конкретный сниппет.  
Также есть возможность зарегистрироваться и залогиниться.

Если пользователь залогинен, то ему открывается возможность создавать сниппеты.  
В общем, идея простая и довольно понятная.

2. **Code**

А теперь давайте посмотрим на то, как это устроено в коде.

Приложение представляет собой пакет `main` в директории `cmd/web/`.  
Плюс несколько внутренних пакетов:
- `assert`
- `models`
	- `models/mocks` (для тестов)
- `validator`

и пакет `ui`, реализующий встраивание статических файлов в итоговый бинарник.

Сам пакет `main` представляет собой внутреннюю инфраструктуру приложения, и его центром является структура `application`:
```go
type application struct {
	logger         *slog.Logger
	snippets       models.SnippetModelInterface
	users          models.UserModelInterface
	templateCache  map[string]*template.Template
	formDecoder    *form.Decoder
	sessionManager *scs.SessionManager
}
```

Далее, на старте приложения отрабатывает функция `main`.

В `main` собираются данные для структуры `application`:
```go
app := &application{
	logger:         logger,
	snippets:       &models.SnippetModel{DB: db},
	users:          &models.UserModel{DB: db},
	templateCache:  templateCache,
	formDecoder:    formDecoder,
	sessionManager: sessionManager,
}
```

И стартует веб-сервер, которому передается созданный нами маршрутизатор с установленным набором маршрутов и соответствующих хендлеров:
```go
srv := &http.Server{
	Addr:         *addr,
	Handler:      app.routes(),
	ErrorLog:     slog.NewLogLogger(logger.Handler(), slog.LevelError),
	TLSConfig:    tlsConfig,
	IdleTimeout:  time.Minute,
	ReadTimeout:  5 * time.Second,
	WriteTimeout: 10 * time.Second,
}
err = srv.ListenAndServeTLS("./tls/cert.pem", "./tls/key.pem")
```

То есть есть слой кода, отрабатывающий в момент запуска бинарника — он формирует приложение.

И, соответственно, есть слой, который работает по запросу (в нашем случае — по HTTP-запросу).

В общем, вы сможете заложить основы построения архитектуры приложения, работы с БД, логирования, работы с шаблонами, middleware, сессиями, работы с формами, защиты сервера, тестирования и еще ряда менее ярких, но тем не менее необходимых тем. 

## Резюме

На этом я завершу обзор.

Лишь уточню, что вместе с книгой я приложу репозиторий `demo-app`, где каждая ветка — это отдельная глава, содержащая текущие изменения приложения.

В левом верхнем углу каждой страницы книги есть маркировка текущей главы вида:

(Слайд №1)
```
Chapter 6.2.
```
И если вы хотите попасть на состояние проекта, актуальное для этой главы, то можете просто выполнить команду:
```bash
git checkout chapter-6.2
```
Я добавил ее для тех, кто не хочет тратить время на копирование и вставку кода из книги, а хочет просто и удобно, переключая ветки, отслеживать изменения в коде по ходу прочтения.

В общем, если этот материал показался вам интересным, то не забудьте про лайк и подписку.  
Ну и, конечно, подписывайтесь в ТГ.

Думаю, что в ближайшие месяц или два выложу продолжение, то есть следующую часть книги, в которой уже содержатся более продвинутые темы.

И также планирую в это время выложить еще одно видео, в котором я разбираю Go для back-end-разработки.

Хорошо, на этом я закончу. Всем спасибо за просмотр и успехов в изучении Go.
