Вывод:

- error — это значение, сигнализирующее вызывающему коду о проблеме.  
Оно не завершает программу само по себе и является частью обычного потока выполнения.

- panic — механизм аварийной раскрутки стека, который обычно используется при 
нарушении инварианта программы или возникновении невозможного состояния.

- os.Exit — механизм принудительного завершения процесса с заданным кодом возврата.  
Он завершает процесс немедленно: `defer` не выполняются, стек не раскручивается.